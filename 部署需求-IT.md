# 部署需求 - 提交给 IT / 运维

> 用途：将「OS 档案馆」静态网站从第三方托管（GitHub Pages）迁移到公司域名下自主托管。
> 本文档可直接转发给 IT / 运维团队。把 `<...>` 占位替换为真实信息即可。

---

## 【需求】将静态网站迁移到公司域名自主托管

**背景**：我们有一个纯静态网站（手机系统升级资讯展示站，无后端、无数据库），目前托管在第三方（GitHub Pages）。现需迁移到公司域名下。

### 一、需要申请的资源

1. **域名**：请提供一个独立子域名（如 `os.<公司名>.com`），配置 HTTPS 证书，并解析到下方 CDN。
2. **静态托管**：请提供一个对象存储 bucket + CDN 加速域名（腾讯云 COS / 阿里云 OSS + CDN 均可）。整站是纯静态文件，原样上传到一个根目录即可，无需容器或服务器。
3. **上传权限**：请给一个对该 bucket 具备"上传 / 覆盖 / 删除"权限的 AK/SK（用于我方 CI 自动发布）；若不便给云账号密钥，也可由我方交付打包好的文件、贵方手动上传。
4. **容量与流量**：主站本体很小（约几十 MB）；另需约 **1.1GB+** 空间存放"官方系统官网离线副本"和宣传视频，并请确认对外访问流量额度充足。

### 二、CDN / 存储必须支持的配置

5. **缓存策略**（重要，否则更新后用户看不到新内容）：
   - 路径 `/assets/*` 与 `/scripts/*`：`Cache-Control: public, max-age=31536000, immutable`（长缓存一年、不可变）
   - 其余路径：`Cache-Control: public, max-age=0, must-revalidate`（不缓存、每次校验）
6. **跨域（CORS）**：允许主站域名跨域读取资源（站内以 iframe / 异步方式加载官方存档与视频）。
7. **Range 请求**：CDN 需透传 `Accept-Ranges / Content-Range / Content-Length`，支持视频断点续传与拖拽进度。

### 三、交付方式

我方负责把网站构建为静态文件并上传；贵方只需将域名、存储、上述 CDN 配置就绪。上线前我方会做一轮完整验收（功能 + 性能 + 跨域视频）。

---

## IT 可能追问的统一回复

- **"为什么 `/assets/` 要缓存一年？"**
  → 网站的图片/JS/CSS 文件名都带唯一编码，更新等于换文件名，所以旧文件可以永久缓存、不影响更新；而页面文件不能缓存，否则改了内容用户看不到。

- **"AK/SK 不能给外部 CI"**
  → 那就开个能收文件的上传通道（控制台账号或内网上传页），我方交付打包好的文件夹，贵方拖进去即可。两种都行。

- **"这网站是干啥的、重不重？"**
  → 纯展示型静态站，没后台没数据库，不占计算资源；主要是那 1.1GB 的官方副本和视频占空间，访问压力也不大。

- **"要不要分开两个 bucket？"**
  → 主站和那 1.1GB 大文件放同个或不同 bucket 都行，按习惯来，只要满足上面的缓存 / 跨域 / Range 三点。

---

## 我方（Remy / Ricky）待办

- [ ] 拿到真实公司域名（如 `os.<公司名>.com`）
- [ ] 拿到 COS/OSS 的 bucket + Endpoint + 具备写权限的 AK/SK（或确认由 IT 手动上传）
- [ ] 确认 1.1GB 官方存档原始素材是否还在本地（README 写从 `/Users/jingzhan.chen/Workbuddy/OS网页存档` 导入）
- [ ] 技术侧：改构建配置（域名 / 路径 / 官方存档地址 / 视频地址）、改自动发布流程、配缓存规则、上线前验收
