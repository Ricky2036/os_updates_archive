# 内网 / 公司网页托管 - 通用申请（多项目）

> 用途：除「OS 档案馆」外，还有其他纯静态网页要放到公司（内网或公司域名）时，**一次性申请"可复用的网页托管能力"**，而不是每个项目单独提一次需求。
> 本文档可直接转发给 IT / 运维。把 `<...>` 占位替换为真实信息即可。

---

## 申请思路（先讲清楚再发）

不要一个项目提一次需求，改成：**申请一个"标准内部静态网页托管"能力**——一次把存储 + CDN（或内网等效）申请下来，以后每个网页只是往里加一个文件夹 / 子站点，无需重复走流程。

「OS 档案馆」可作为第一个入驻项目。

---

## 给 IT 的通用需求（可转发）

> **【需求】内部纯静态网页的统一托管能力**
>
> **背景**：我们有若干个**纯静态网页**（无后端、无数据库，只是展示型页面），需要放到公司（内网或公司域名）下，且后续还会有更多类似页面。希望申请一套**可复用的托管方案**，而非每加一个页面都重新走流程。
>
> **一、需要申请的资源**
> 1. **入口**：请提供一个统一入口（二选一，推荐方案 A）：
>    - **方案 A（子路径，推荐）**：一个域名（如 `intranet.<公司名>.com` 或内网等效地址），各项目放在不同子路径下，如 `/projA`、`/projB`。
>    - **方案 B（子域名）**：每个项目一个独立子域名，如 `projA.<公司名>.com`。
> 2. **静态托管**：对象存储 bucket + CDN 加速（腾讯云 COS / 阿里云 OSS + CDN 或内网等效）。整站纯静态文件，原样上传到一个根目录下的各项目文件夹即可，无需容器或服务器。
> 3. **上传权限**：对 bucket 具备"上传 / 覆盖 / 删除"权限的 AK/SK（用于我方 CI 自动发布）；若不便给密钥，也可由我方交付打包好的文件、贵方手动上传。
> 4. **容量与流量**：单个项目通常很小（几十 MB ~ 1GB 不等），请按需预留空间并确认流量额度。
>
> **二、CDN / 存储必须支持的配置**
> 5. **缓存策略**（重要，否则更新后用户看不到新内容）：
>    - 路径 `<项目>/assets/*` 与 `<项目>/scripts/*`：`Cache-Control: public, max-age=31536000, immutable`
>    - 其余路径：`Cache-Control: public, max-age=0, must-revalidate`
> 6. **跨域（CORS）**：允许主站域名跨域读取资源（站内以 iframe / 异步方式加载子资源时必需）。
> 7. **Range 请求**：CDN 需透传 `Accept-Ranges / Content-Range / Content-Length`，支持视频断点续传与拖拽进度。
>
> **三、访问范围（按实际情况勾选）**
> - [ ] **仅内网可访问**（公司网络 / VPN 内可见，不暴露公网）
> - [ ] **对公网公开**
> - 若为内网：请配置相应的访问控制（如仅限公司 IP / 登录后可见），内部仍建议使用 https。
>
> **四、交付方式**
> 我方负责把网页构建为静态文件并上传到对应项目路径；贵方只需将入口、存储、上述 CDN 配置就绪。新项目入驻时，我方只需新建一个子路径文件夹，贵方无需重复审批。

---

## IT 可能追问的统一回复

- **"子路径和子域名选哪个？"**
  → 推荐子路径：只申请一次入口，以后加项目只是加文件夹，IT 几乎零额外工作。子域名每加一个都要配 DNS + 证书，成本高。

- **"为什么 `/assets/` 要缓存一年？"**
  → 网页的图片/JS/CSS 文件名都带唯一编码，更新等于换文件名，旧文件可永久缓存不影响更新；页面文件不能缓存，否则改了内容用户看不到。

- **"AK/SK 不能给外部 CI"**
  → 开个能收文件的上传通道（控制台账号或内网上传页），我方交付打包好的文件夹，贵方拖进去即可。

- **"这些网页重不重？"**
  → 都是纯展示型静态站，没后台没数据库，不占计算资源；主要是图片/视频占空间，访问压力也不大。

---

## 我方（Remy / Ricky）待办

- [ ] 确认每个网页是"仅内网"还是"对公网公开"
- [ ] 拿到统一入口域名（子路径方案下的根域名）
- [ ] 拿到 COS/OSS 的 bucket + Endpoint + 具备写权限的 AK/SK（或确认由 IT 手动上传）
- [ ] 列出所有要入驻的网页清单（OS 档案馆为第一个）
- [ ] 技术侧：为每个项目配好"子路径前缀 / 域名 / 官方存档地址 / 视频地址"、改自动发布流程、配缓存规则、上线前验收
